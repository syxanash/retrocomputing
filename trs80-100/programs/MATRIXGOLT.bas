0 CLS
10 INPUT "ENTER COLUMN SIZE"; COLS
20 INPUT "ENTER ROWS SIZE"; ROWS
30 GRID = COLS * ROWS
40 DIM CGEN(ROWS,COLS)
50 DIM NGEN(ROWS,COLS)
60 INPUT "ENTER RANDOM SEED (1 TO 5 DIGITS)"; SEED%
70 LF = INT(RND(SEED%*-2))
80 FOR I = 0 TO ROWS-1
90 FOR J = 0 TO COLS-1
100 LF = INT(2*RND(1))
110 CGEN(I,J) = LF
120 NGEN(I,J) = CGEN(I,J)
130 NEXT J
140 NEXT I
150 CLS
160 BEEP
170 FOR I = 0 TO ROWS-1
180 FOR J = 0 TO COLS-1
190 IF NGEN(I, J) = 0 THEN PRINT " "; ELSE PRINT "@";
200 CGEN(I, J) = NGEN(I, J)
210 NEXT J
220 PRINT ""
230 NEXT I
240 FOR I = 0 TO ROWS-1
250 FOR J = 0 TO COLS-1
260 NGB = 0
270 A = CGEN(((((I-1) MOD ROWS) + ROWS) MOD ROWS), ((((J-1) MOD COLS) + COLS) MOD COLS))
280 B = CGEN(((((I-1) MOD ROWS) + ROWS) MOD ROWS), (((J MOD COLS) + COLS) MOD COLS))
290 C = CGEN(((((I-1) MOD ROWS) + ROWS) MOD ROWS), ((((J+1) MOD COLS) + COLS) MOD COLS))
300 D = CGEN((((I MOD ROWS) + ROWS) MOD ROWS), ((((J-1) MOD COLS) + COLS) MOD COLS))
310 E = CGEN((((I MOD ROWS) + ROWS) MOD ROWS), ((((J+1) MOD COLS) + COLS) MOD COLS))
320 F = CGEN(((((I+1) MOD ROWS) + ROWS) MOD ROWS), ((((J-1) MOD COLS) + COLS) MOD COLS))
330 G = CGEN(((((I+1) MOD ROWS) + ROWS) MOD ROWS), (((J MOD COLS) + COLS) MOD COLS))
340 H = CGEN(((((I+1) MOD ROWS) + ROWS) MOD ROWS), ((((J+1) MOD COLS) + COLS) MOD COLS))
350 NGB = A + B + C + D + E + F + G + H
360 IF NGB < 2 THEN NGEN(I, J) = 0
370 IF NGB > 3 THEN NGEN(I, J) = 0
380 IF NGEN(I, J) = 0 AND NGB = 3 THEN NGEN(I, J) = 1
390 NEXT J
400 NEXT I
410 GOTO 150